import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    principalPage: {
        backgroundColor: '#E0DBA0',
        width: '100%',
        height: '100%'
    }

})

export default styles