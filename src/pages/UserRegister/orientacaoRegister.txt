/*
import addNewUser from "../../services/addNewUser"

const RegisterScreen = () => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [userID, setUserID] = useState('');

  const handleRegister = () => {
    console.log(email,password)
    setUserID(addNewUser(email, password));
    //Esse userID deve ser passado como parâmetro para as páginas seguintes
    // se não me engano é assim:
    // export default function Login({ navigation , route })
    // navigation.navigate("Rota-desejada", { userID: userID })
    }
    onPress={() => {
                        handleRegister(email, password);}
*/